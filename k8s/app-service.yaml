apiVersion: v1

kind: Service

metadata:
  name: pharmacy-api-service

spec:
  selector:
    app: pharmacy-api

  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000

  type: LoadBalancer